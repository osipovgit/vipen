---
- name: Read available TCP congestion controls
  ansible.builtin.command: sysctl -n net.ipv4.tcp_available_congestion_control
  register: network_tuning_tcp_available_congestion_control
  changed_when: false
  when:
    - network_tuning_sysctl_tuning_enabled | bool
    - network_tuning_sysctl_settings['net.ipv4.tcp_congestion_control'] is defined
    - not ansible_check_mode

- name: Verify requested TCP congestion control is available
  ansible.builtin.assert:
    that:
      - network_tuning_sysctl_settings['net.ipv4.tcp_congestion_control'] in network_tuning_tcp_available_congestion_control.stdout.split()
    fail_msg: "Requested TCP congestion control is not available on this kernel."
  when:
    - network_tuning_sysctl_tuning_enabled | bool
    - network_tuning_sysctl_settings['net.ipv4.tcp_congestion_control'] is defined
    - not ansible_check_mode

- name: Load nf_conntrack on boot when conntrack tuning is enabled
  ansible.builtin.copy:
    dest: /etc/modules-load.d/nf_conntrack.conf
    owner: root
    group: root
    mode: "0644"
    content: "nf_conntrack\n"
  when:
    - network_tuning_sysctl_tuning_enabled | bool
    - network_tuning_sysctl_settings['net.netfilter.nf_conntrack_max'] is defined

- name: Load nf_conntrack now when conntrack tuning is enabled
  ansible.builtin.command: modprobe nf_conntrack
  changed_when: false
  when:
    - network_tuning_sysctl_tuning_enabled | bool
    - network_tuning_sysctl_settings['net.netfilter.nf_conntrack_max'] is defined
    - not ansible_check_mode

- name: Configure VPN network sysctl tuning
  ansible.builtin.template:
    src: 99-vpn-performance.conf.j2
    dest: /etc/sysctl.d/99-vpn-performance.conf
    owner: root
    group: root
    mode: "0644"
  register: network_tuning_sysctl_config
  when: network_tuning_sysctl_tuning_enabled | bool

- name: Apply effective VPN network sysctl tuning
  ansible.builtin.command: "sysctl -w {{ item.key }}={{ item.value }}"
  loop: "{{ network_tuning_sysctl_settings | dict2items }}"
  changed_when: false
  when:
    - network_tuning_sysctl_tuning_enabled | bool
    - not ansible_check_mode

- name: Read effective VPN network sysctl tuning
  ansible.builtin.command: "sysctl -n {{ item.key }}"
  loop: "{{ network_tuning_sysctl_settings | dict2items }}"
  register: network_tuning_sysctl_effective
  changed_when: false
  when:
    - network_tuning_sysctl_tuning_enabled | bool
    - not ansible_check_mode

- name: Verify effective VPN network sysctl tuning
  ansible.builtin.assert:
    that:
      - item.stdout == item.item.value
    fail_msg: "{{ item.item.key }} is {{ item.stdout }}, expected {{ item.item.value }}"
  loop: "{{ network_tuning_sysctl_effective.results | default([]) }}"
  when:
    - network_tuning_sysctl_tuning_enabled | bool
    - not ansible_check_mode

- name: Detect default route interface
  ansible.builtin.shell: |
    set -o pipefail
    ip -o route get 1.1.1.1 | awk '{for (i=1; i<=NF; i++) if ($i == "dev") {print $(i+1); exit}}'
  args:
    executable: /bin/bash
  register: network_tuning_default_route_interface
  changed_when: false
  when:
    - network_tuning_sysctl_tuning_enabled | bool
    - network_tuning_apply_qdisc_to_default_route | bool
    - network_tuning_sysctl_settings['net.core.default_qdisc'] is defined
    - network_tuning_sysctl_settings['net.core.default_qdisc'] == 'fq'
    - not ansible_check_mode

- name: Read active qdisc on default route interface
  ansible.builtin.command: "tc qdisc show dev {{ network_tuning_default_route_interface.stdout }} root"
  register: network_tuning_default_route_qdisc
  changed_when: false
  when:
    - network_tuning_sysctl_tuning_enabled | bool
    - network_tuning_apply_qdisc_to_default_route | bool
    - network_tuning_default_route_interface.stdout | default('') | length > 0
    - not ansible_check_mode

- name: Apply fq qdisc to default route interface
  ansible.builtin.command: "tc qdisc replace dev {{ network_tuning_default_route_interface.stdout }} root fq"
  when:
    - network_tuning_sysctl_tuning_enabled | bool
    - network_tuning_apply_qdisc_to_default_route | bool
    - network_tuning_default_route_interface.stdout | default('') | length > 0
    - network_tuning_default_route_qdisc.stdout | default('') is not match('^qdisc fq ')
    - not ansible_check_mode

- name: Verify active qdisc on default route interface
  ansible.builtin.command: "tc qdisc show dev {{ network_tuning_default_route_interface.stdout }} root"
  register: network_tuning_default_route_qdisc_after
  changed_when: false
  when:
    - network_tuning_sysctl_tuning_enabled | bool
    - network_tuning_apply_qdisc_to_default_route | bool
    - network_tuning_default_route_interface.stdout | default('') | length > 0
    - not ansible_check_mode

- name: Assert active fq qdisc on default route interface
  ansible.builtin.assert:
    that:
      - network_tuning_default_route_qdisc_after.stdout is match('^qdisc fq ')
    fail_msg: "Default route qdisc is {{ network_tuning_default_route_qdisc_after.stdout }}, expected fq."
  when:
    - network_tuning_sysctl_tuning_enabled | bool
    - network_tuning_apply_qdisc_to_default_route | bool
    - network_tuning_default_route_interface.stdout | default('') | length > 0
    - not ansible_check_mode
